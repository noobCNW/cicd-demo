{
  "name": "cicd-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Demo app for cicd-platform build & deploy test",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/health.test.js",
    "build": "mkdir -p dist && cp -r src package.json dist/ && echo build-ok > dist/BUILD_INFO"
  },
  "engines": { "node": ">=18" }
}
